DEFAULTS: &DEFAULTS
  email: $LABEL@example.com
  kind: internal
  main_user: one
  name: Group $LABEL
  reference_id: <%= ActiveRecord::FixtureSet.identify :one %>
  short_name: $LABEL

one:
  <<: *DEFAULTS

two:
  <<: *DEFAULTS

external:
  <<: *DEFAULTS
  kind: external
  main_user: two
  users: two

field_service:
  <<: *DEFAULTS
  kind: field_service_team

internal:
  <<: *DEFAULTS
  users: one
