DEFAULTS: &DEFAULTS
  email: $LABEL@example.com
  kind: external
  main_user: one
  name: Group $LABEL
  reference_id: <%= ActiveRecord::FixtureSet.identify :one %>
  short_name: $LABEL

one:
  <<: *DEFAULTS

two:
  <<: *DEFAULTS

internal:
  main_user: one
  users: one

external:
  main_user: two
  users: two
