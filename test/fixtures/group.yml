DEFAULTS: &DEFAULTS
  email: $LABEL@example.com
  kind: internal
  main_user: one
  name: Group $LABEL
  reference_id: <%= ActiveRecord::FixtureSet.identify :one %>
  short_name: $LABEL

one:
  <<: *DEFAULTS

two:
  <<: *DEFAULTS

external:
  <<: *DEFAULTS
  kind: external

field_service: 
  <<: *DEFAULTS
  kind: field_service_team
