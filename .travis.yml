addons:
  apt:
    packages:
      - postgresql-12-postgis-3
  postgresql: 12

before_install:
  - sudo cp /etc/postgresql/{9.6,12}/main/pg_hba.conf
  - cat /etc/postgresql/{9.6,12}/main/pg_hba.conf
  - systemctl status postgresql.service
  - journalctl -xe
  - sudo service postgresql restart 12 main

before_script:
  - psql -U postgres -c "create extension postgis"
  - psql -U postgres -c 'create database travis_ci_test'
  - cp config/database.yml.travis config/database.yml

cache:
  bundler: true

env:
  global:
    - PGPORT=5433

language: ruby

script:
  - bundle exec rubocop

services:
  - postgresql
