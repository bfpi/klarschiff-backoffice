Sehr geehrte(r) Nutzer/-in von <%= Settings::Instance.name %>,

um Ihre Missbrauchsmeldung zu bestätigen rufen Sie bitte folgende URL auf: <%= @url %>

Diese URL funktioniert für <%= Jobs::Issue.deletion_deadline %> Stunden und kann einmalig aufgerufen werden. Nach Ablauf dieser Frist wird Ihre Missbrauchsmeldung automatisch hinfällig.

Vielen Dank und mit freundlichen Grüßen

Ihr <%= Settings::Instance.name %>-Team
