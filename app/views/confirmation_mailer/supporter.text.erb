Sehr geehrte(r) Nutzer/-in von <%= Settings::Instance.name %>,

um Ihre Untersützung zu bestätigen rufen Sie bitte folgende URL auf: <%= @url %>

Diese URL funktioniert für <%= Jobs::Issue.deletion_deadline %> Stunden und kann einmalig aufgerufen werden. Nach Ablauf dieser Frist wird Ihre Unterstützung automatisch hinfällig.

Vielen Dank und mit freundlichen Grüßen

Ihr <%= Settings::Instance.name %>-Team
