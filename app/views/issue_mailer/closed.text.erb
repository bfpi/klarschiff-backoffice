Sehr geehrte(r) Nutzer/-in von <%= Settings::Instance.name %>,

die Bearbeitung Ihrer Meldung #<%= @issue.id %> vom <%= l(@issue.created_at, format: :elaborate) %>, ist abgeschlossen, und zwar mit folgendem Status:

<%= status_with_optional_status_note @issue %>

Nachfolgend können Sie nochmals die wichtigsten Daten Ihrer Meldung in kompakter Form einsehen:

<%= render partial: 'issue', object: @issue %>

Vielen Dank und mit freundlichen Grüßen

Ihr <%= Settings::Instance.name %>-Team
