<%= t :salutation_user, name: Settings::Instance.name %>

<%= t('.message', issue_id: @issue.id, issue_created_at: l(@issue.created_at, format: :elaborate)) %>

<%= render partial: 'issue', locals: { issue: @issue } %>

<%= t :best_regards, name: Settings::Instance.name %>
