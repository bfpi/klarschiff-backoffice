Folgende Vorgänge weisen zwar nicht mehr den Status "offen" auf, wurden bisher aber dennoch nicht akzeptiert. Bitte prüfen Sie jeweils die Zuständigkeit; anschließend akzeptieren Sie diese bitte oder leiten den Vorgang weiter:

Nummer    aktuelle Zuständigkeit     Typ     Status      nicht akzeptiert seit
******    **********************     ***     ******      *********************
<% issues.each do |issue| -%>
<%= [issue.id, issue.group, MainCategory.human_enum_name(:kind, issue.kind),
     Issue.human_enum_name(:status, issue.status), l(issue.responsibility_since)].join("\t\t\t") %>
<% end -%>
