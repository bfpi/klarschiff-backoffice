Folgende Meldungen weisen zwar nicht mehr den Status „<%= Issue.human_enum_name :status, :received %>“ auf, wurden bisher aber dennoch nicht akzeptiert.
Bitte prüfen Sie jeweils die Zuständigkeit; anschließend akzeptieren Sie diese bitte oder leiten die Meldungen weiter:

Nummer<%= "\t" %>Zuständigkeit<%= "\t" %>Typ<%= "\t" %>Status<%= "\t" %>nicht akzeptiert seit
******<%= "\t" %>*************<%= "\t" %>***<%= "\t" %>******<%= "\t" %>*********************
<% issues.each do |issue| -%>
<%= [issue.id, issue.group, MainCategory.human_enum_name(:kind, issue.kind),
     Issue.human_enum_name(:status, issue.status),
     (issue.responsibility_since ? l(issue.responsibility_since, format: :elaborate) : '')].join("\t") %>
<% end -%>
