Sehr geehrte(r) Bearbeiter/-in von Meldungen in <%= Settings::Instance.name %>,

innerhalb der letzten <%= JobSettings::Issue.delegation_deadline %> Stunden wurden Meldungen an Sie delegiert. Bitte bearbeiten Sie diese Meldungen:

Daten der neuen delegierten Meldungen
************************************
<% @issues.each do |issue| -%>
<%= render partial: 'issue', object: issue, locals: { issue_link: edit_delegation_url(issue) } -%>
************************************
<% end -%>

Vielen Dank und mit freundlichen Grüßen

Ihr <%= Settings::Instance.name %>-Team
