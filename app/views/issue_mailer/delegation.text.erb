Sehr geehrte(r) Bearbeiter/-in von Vorgängen in <%= Settings::Instance.name %>,

innerhalb der letzten <%= JobSettings::Issue.delegation_deadline %> Stunden wurden Vorgänge an Sie delegiert. Bitte bearbeiten Sie diese Vorgänge:

Daten der neuen delegierten Vorgänge
************************************
<% @issues.each do |issue| -%>
<% auth_code = AuthCode.find_by(issue: issue, group: issue.delegation).uuid if @with_auth_code -%>
<% url = @with_auth_code ? delegation_url(issue, auth_code: auth_code) : delegation_url(issue) -%>
<%= render partial: 'issue', object: issue, locals: { issue_link: url } -%>
************************************
<% end -%>

Vielen Dank und mit freundlichen Grüßen

Ihr <%= Settings::Instance.name %>-Team
