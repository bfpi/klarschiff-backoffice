Sehr geehrte(r) Bearbeiter/-in von Vorgängen in <%= Settings::Instance.name %>,

es wurde ein Vorgang an Sie zugewiesen. Klicken Sie auf den folgenden Link, um die Zuweisung zu bestätigen:

Daten des zugewiesenen Vorgangs
************************************
<%= render partial: 'issue', object: @issue, locals: { issue_link: issues_url(auth_code: @auth_code.uuid) } -%>
************************************

Vielen Dank und mit freundlichen Grüßen

Ihr <%= Settings::Instance.name %>-Team
