Folgende Meldungen mit dem Status "<%= Issue.human_enum_name :status, :in_process %>" sind seit mindestens <%= days %> Tagen unverändert.
Bitte geben Sie den Bürgerinnen und Bürgern jeweils eine kurze Info zum Bearbeitungsstand mittels der öffentlichen Statusinformation oder schließen Sie die Meldung ab, falls dies bislang einfach vergessen wurde:

Nummer<%= "\t" %>aktuelle Zuständigkeit<%= "\t" %>Typ<%= "\t" %>unverändert seit
******<%= "\t" %>**********************<%= "\t" %>***<%= "\t" %>****************
<% issues.each do |issue| -%>
<%= [issue.id, issue.group, MainCategory.human_enum_name(:kind, issue.kind), l(issue.status_since)].join("\t") %>
<% end -%>
