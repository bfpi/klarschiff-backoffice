Folgende Meldungen mit dem Status „<%= Issue.human_enum_name :status, :received %>“ sind seit mindestens <%= days %> Tag(en) zugewiesen.
Bitte prüfen Sie jeweils die Zuständigkeit; anschließend akzeptieren Sie diese bitte oder leiten die Meldung weiter. Falls Sie die Zuständigkeit akzeptieren, so führen Sie bitte außerdem die (Erst-)Sichtung durch, falls diese noch nicht erfolgt ist:

Nummer<%= "\t" %>Zuständigkeit<%= "\t" %>Typ<%= "\t" %>zugewiesen seit
******<%= "\t" %>*************<%= "\t" %>***<%= "\t" %>***************
<% issues.each do |issue| -%>
<%= [issue.id, issue.group, MainCategory.human_enum_name(:kind, issue.kind),
     (issue.responsibility_since ? l(issue.responsibility_since, format: :elaborate) : '')].join("\t") %>
<% end -%>
