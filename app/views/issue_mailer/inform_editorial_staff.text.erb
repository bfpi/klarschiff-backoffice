<%= t :salutation_editor, name: Settings::Instance.name %>

<%= t('.note_important_advices') %>
<% @issues.each do |key, issues| -%>

<%= render partial: key.to_s, locals: { issues:, days: @days[key] } if issues.present? %>
<% end -%>

<%= t('.message', name: Settings::Instance.name) %>

<%= t :best_regards, name: Settings::Instance.name %>
