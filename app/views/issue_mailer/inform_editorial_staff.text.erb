Sehr geehrte(r) Bearbeiter/-in,

es gibt folgende wichtige Hinweise zum Bearbeitungsstand:
<% @issues.each do |key, issues| -%>
  <%= render partial: key.to_s, locals: { issues: issues } if issues.present? %>
<% end -%>

Wir möchten Sie bitten diesen Hinweisen nachzugehen.
Eine kurzfristige Reaktion auf die Vorgänge in <%= Settings::Instance.name %> trägt zum positiven Image der Verwaltung bei.
