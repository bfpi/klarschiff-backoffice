Folgende Ideen mit dem Status "<%= Issue.human_enum_name :virtual_status, :open %>" haben ihre Erstsichtung seit mindestens <%= days %> Tagen hinter sich,
weisen aber bisher noch nicht die notwendige Anzahl von <%= Settings::Vote.min_requirement %> Unterstützungen auf.
Falls Sie die jeweilige Idee abschließen möchten, so setzen Sie diese bitte auf den Status "<%= Issue.human_enum_name :status, :not_solvable %>" und geben Sie den Bürgerinnen und Bürgern eine kurze Begründung mittels der öffentlichen Statusinformation:

Nummer<%= "\t" %>aktuelle Zuständigkeit<%= "\t" %>Unterstützungen<%= "\t" %>Erstsichtungszeitpunkt
******<%= "\t" %>**********************<%= "\t" %>***************<%= "\t" %>**********************
<% issues.each do |issue| -%>
<%= [issue.id, issue.group, issue.supporters.count, "\t", l(issue.status_since)].join("\t") %>
<% end -%>
