<div class="row">
  <div class="col-sm">
    <div class="card">
      <div class="card-header">
        Nutzerwechsel
      </div>
      <div class="card-body">
        <%= form_for :login, url: logins_path, method: :put do |form| -%>
          <div class="row">
            <%= form.label :user, User.model_name.human(count: 2), class: 'col-2 col-form-label' %>
            <div class="col-4">
              <%= form.select :user_id, users, {}, class: 'form-select' %>
            </div>
          </div>
          <div class="col-4 offset-md-2">
            <%= form.submit 'Übernehmen', class: 'btn btn-primary' %>
          </div>
        <% end -%>
      </div>
    </div>
  </div>
</div>
